#include <iostream>
#include <string>
#include <fstream>
#include <string>
using namespace std;

class Utility {

private:
	
	ifstream myfile;
	
	string outputFile = "";

	string input[100];
	
	int numer_of_lines;

public:

	encodedConnection(Client, Client, encodedServer, string);

};
